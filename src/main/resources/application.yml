spring:
  application:
    name: shopping

  datasource:
    url: jdbc:postgresql://localhost:5432/shopping_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
      show-sql: true
      hibernate:
        ddl-auto: create

